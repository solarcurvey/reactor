<!-- GENERATED by scripts/sync-docs.ts from docs/version.json + deployments/registry.json. Do not edit by hand. -->

# Versioning

> Protocol release **0.3.4** (`v0.3.4`, 2026-09-12). Factory **V1** is immutable and is not this number.

pre-audit local/testnet — not mainnet. Frontend production observability on main ff444cb (squash-merged #81 / #69 docs-only path filter after #46 / #39 after #75 Restricted-access UX / #44 E2E release gate / #70 sanctions freshness / #79 CI evidence / #68 operator-policy / #67 geo / #66 OFAC / #49 UI QA / #42 full GitHub CI extras / #50 indexed board + page-budget / #58 typecheck / #73 three-tier CI / #77 / #76 / #74 / #59 / #47 CSP). Product UI identity is Industrial Forge (Direction C, #55 — stays open for audit). Sentry-or-equivalent with release SHA + first-party source-map symbolication + configured-DSN vendor proof (in-process store mock; live org still post-merge). Redacted API/RPC/wallet/quote/SSE/tx/media/simulation telemetry, Web Vitals, alert runbook, traceId correlation, exact env/chain/build tags. POST /api/telemetry is 40/10s/IP in genuine production; review-fixture / REACTOR_TELEMETRY_RELAXED artifacts raise the cap so web-qa does not starve ingest. Exact-head CI job obs-ui folded into ci.yml (full/main; production next start). #75 /restricted + policy.ensureProof, #44 e2e-release-gate, #70 sanctions-ops docs/nav, #79 #17 evidence, and #68 recovered-wallet proof stay. #67 geo + #66 OFAC + #49 web-qa + #42 docs-links / Playwright web / safe-genesis stay. #39 stays open until post-merge live vendor verify. Protocol stays 0.3.4 unless #54 0.4.0 lands first — then rebase and do not restore 0.3.4. Economics and Factory V1 unchanged. Not audited. No mainnet.

## Two version numbers

| Kind | What it is | Source of truth | This HEAD |
| --- | --- | --- | --- |
| **Protocol release** | Overall REACTOR software + docs release (semver) | `docs/version.json` → `protocolVersion` | **0.3.4** |
| **Factory version** | Immutable on-chain factory label for **new launches** | `ReactorFactory.FACTORY_VERSION` | **V1** (`1`) |

Factory **V1 stays V1 forever**. A different fee split, supply, Dev Buy cap, or curve is a **new factory deploy** (V2, V3, …), authorized by Guardian for new launches only. Existing V1 tokens are untouched. See `FACTORY_VERSIONING.md`.

Bumping `protocolVersion` does **not** change Factory V1. Do not retag Factory V1 as a protocol version.

## Single source of truth

Edit **`docs/version.json`** only. Then run `pnpm docs:gen`. CI (`pnpm docs:check`) fails if:

- root `package.json` `version` ≠ `protocolVersion`
- `releaseTag` ≠ `v` + `protocolVersion`
- `CHANGELOG.md` has no heading for this protocol version
- `factoryVersion` / label ≠ Solidity `FACTORY_VERSION`
- generated `docs/versioning.md` / `docs/deployments.md` are stale
- tracked docs / `BUILD_REPORT.md` still contain `<<<<<<<` / `>>>>>>>` conflict markers
- fee / supply / Dev Buy / ticker lock / factory labels in docs drifted from code
- `deployments/local.json` copies (web + indexer) drifted
- deployment tables omit a recorded local address, or invent a mainnet (5042) address

## Tagging convention

Every **production** protocol release:

1. Bump `docs/version.json` (`protocolVersion`, `releaseTag`, `releaseDate`).
2. Add a `CHANGELOG.md` section for that version.
3. Run `pnpm docs:gen` and `pnpm docs:check`.
4. Commit the docs + version files in the **same** change as the code.
5. Create an annotated git tag matching `releaseTag`:

```
git tag -a v0.3.4 -m "REACTOR protocol 0.3.4"
git push origin v0.3.4
```

Rules:

- Tags are `vMAJOR.MINOR.PATCH` (leading `v`).
- Never move or reuse a tag.
- Factory versions are **not** git tags.
- Pre-audit / local-only baselines still get a changelog + tag so HEAD is identifiable.
- Do not tag Arc Mainnet deploys. There are none.

## Constants frozen in Factory V1

These are code constants. Changing them is a new factory, not a protocol patch.

| Constant | Code | Docs must say |
| --- | --- | --- |
| Protocol fee | `350` bps | **3.5%** |
| Holders | `200` bps | **2% holders** |
| Top-10 flywheel | `100` bps | **1%** |
| CORE buy+burn | `50` bps | **0.5% CORE** |
| Launch supply | `1,000,000,000` | **1B** |
| Dev Buy cap | `500` bps | **5%** token-out |
| Ticker lock | `86400` s | **24h** global |

## Related

- [Changelog](/docs/changelog)
- [Deployments](/docs/deployments)
- [Docs policy](/docs/policy)
- `CONTRIBUTING.md` · `FACTORY_VERSIONING.md` · `CHANGELOG.md`
